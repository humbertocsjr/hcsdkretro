{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "make hcasm",
            "type": "shell",
            "command": "make -C ${workspaceFolder}/hcasm all"
        },
        {
            "label": "make hclink",
            "type": "shell",
            "command": "make -C ${workspaceFolder}/hclink all"
        },
        {
            "label": "make hcbuild",
            "type": "shell",
            "command": "make -C ${workspaceFolder}/hcbuild all"
        },
        {
            "label": "make retrolang",
            "type": "shell",
            "command": "make -C ${workspaceFolder}/retrolang all"
        }
    ]
}